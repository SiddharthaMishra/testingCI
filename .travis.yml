language: cpp

sudo: false

os:
  -linux

compiler:
  -clang

install: true

add_ons: {
  apt: {
    sources: [
      "ubuntu-toolchain-r-test",
      "llvm-toolchain-trusty"
    ],
    packages: [
      "clang-5.0",
    ]
  }
}

script: scan-build-5.0 g++ test.cpp
  
